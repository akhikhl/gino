apply plugin: 'base' // add "clean" task to the root project.

subprojects {
  apply plugin: 'maven'

  repositories {
    mavenLocal()
    jcenter()
    mavenCentral()
  }

  afterEvaluate {  
    build {
      finalizedBy install
    }
  }
}
