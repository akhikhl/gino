apply plugin: 'java'
apply plugin: 'ginoapp'

// see explanation in README.md
jar {
  manifest {
    from 'src/main/resources/META-INF/MANIFEST.MF'
  }
}

ext {
  programArgs = [ 'hello', 'world!' ]
}

