allprojects {
  group = "org.akhikhl.ghino"
  version = "0.0.1"
}

subprojects {

  repositories {
    mavenLocal()
    mavenCentral()
  }

  apply plugin: "maven"
  apply plugin: "groovy"
  
  dependencies {
    compile "org.codehaus.groovy:groovy-all:1.8.6"
  }
}

