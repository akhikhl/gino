def subDirs = settingsDir.listFiles({ File f ->
    f.isDirectory() && new File(f, 'build.gradle').isFile() && f.name != 'buildSrc'
  } as FileFilter)
subDirs.each { File subdir ->
  include subdir.name
}

